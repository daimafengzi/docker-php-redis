name: Build and Push Multi-Arch PHP Image

on:
  push:
    branches:
      - main
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4  # ✅ 升级到 v4（支持 Node 24）

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3  # ✅ 升级到 v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3  # ✅ 升级到 v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3  # ✅ 升级到 v3

      - name: Extract Metadata
        id: meta
        uses: docker/metadata-action@v5  # ✅ 升级到 v5（明确支持 Node 24）
        with:
          images: |
            ${{ secrets.DOCKER_USERNAME }}/php-redis-alpine
          tags: |
            type=raw,value=latest,enable=${{ github.ref == format('refs/heads/{0}', 'main') }}
            type=ref,event=branch
            type=ref,event=tag

      - name: Build and Push
        uses: docker/build-push-action@v5  # ✅ 升级到 v5
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
